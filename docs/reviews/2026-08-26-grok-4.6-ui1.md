## Critical

**1. Delivery Integrity Report invents “10 tests” and “92% coverage”**  
`app/(app)/project/[projectId]/delivery/page.tsx` — Integrity Report list  

```tsx
{project?.testCases?.length || 10} Automated …
{project?.coverageEstimate?.percentage || 92}% Estimated Coverage
```

When `testCases` is missing/empty or `coverageEstimate` was never written (project jumped here, generation failed, or RAP path never produced `testCases`), the handover screen still shows **10 tests** and **92% coverage** plus a pulsing “Ready for Deployment”. That is a number a customer will put in a steering pack. Trigger: any completed project without those fields — including `handleProceed` sending `cleanCoreScore > 90` straight to Delivery (`dashboard/page.tsx`).

---

## High

**2. Transformation HUD reports 100% whenever nothing needs sign-off**  
`app/(app)/project/[projectId]/transformation/page.tsx` — `currentScore`  

```tsx
const currentScore = signOffFindings.length > 0
  ? Math.min(100, Math.round(initialScore + (100 - initialScore) * (signedOffIds.size / signOffFindings.length)))
  : 100;
```

If `detectFindings` returns no `requiresSignOff` items (clean-looking code, parse miss, or empty `legacyCode`), the shield shows **100%** even when `project.cleanCoreScore` is 40. `signedOffIds` is also component state only — refresh undoes any real sign-offs. Trigger: open Transformation on a project whose findings have `requiresSignOff !== true`.

**3. Analyze UI score is not the signed Run score**  
`app/(app)/project/[projectId]/analyze/page.tsx` — `liveCleanCoreScore` + `renderAnalysisContent`  

```tsx
const weighted = Math.round(coveragePct * 0.6 + standardFitBonus * 0.3 + stored * 0.1);
// …
analysisData.cleanCoreScore = liveCleanCoreScore;
```

The gauge, sticky header, and in-page copy use this client remix (construct coverage + AI “standard fit” + 10% stored). `/api/runs/create` persisted `computedRouteReport.cleanCoreScore`. Confluence export parses `project.analysis` separately and keeps the stored value. Trigger: any analysis with findings or a non-High standard fit — UI vs signed pack vs HTML export disagree.

**4. “Differential ResultSet Test” is a timer with hardcoded rows**  
`transformation/page.tsx` — `runDiffTest`  

```tsx
setTimeout(() => { setDiffTestStatus('success'); … }, 1200);
// then: "S/4HANA: 243 rows fetched"
```

No tenant, no query, no comparison. It also auto-ticks the `complex-sql-join` sign-off. Trigger: click “Run Differential ResultSet Test”. This is fake evidence on a trust-chain screen.

**5. TCO page prints invented euros as a C-level business case**  
`app/(app)/project/[projectId]/tco/page.tsx`  

`scoreAfter` is hardcoded `95`; LoC is `lineCount * 10`; savings/payback/ROI are slider arithmetic. The chrome says “C-Level Executive View”, “Print Business Case”, “Better Practice Mapped”. Trigger: open `/project/:id/tco` and print. Nothing here is measured from the Run.

**6. Analyze fallbacks invent maintenance € and “~40%” upgrade-test cut**  
`analyze/page.tsx` — `bizFallback` in `renderAnalysisContent` and `exportToConfluence`  

```tsx
estimatedMaintenanceCost: Math.max(1500, (100 - score) * 180 + 1200)
cloudRoiSummary: `… reduce core upgrade testing costs by ~40% …`
```

Shown in Business Value Audit and the Confluence HTML whenever Gemini omits `businessValueAnalysis` (parse failure or partial JSON). Trigger: any analysis whose model output lacks that block.

**7. ABAP catalog / evidence engine pulled into client pages**  
`'use client'` pages import `@/lib/abap/catalog-service`, `evidence-model`, `extensibility-router`, `cds-catalog`, `findings-detector`, `class-model-resolver`:

- `analyze/page.tsx` (includes unused `getMergedCatalogVersion`)
- `transformation/page.tsx`
- `design/page.tsx`
- `delivery/page.tsx`

If those modules statically import `lib/abap/generated/*.json` (the artifacts you excluded from this bundle because they are huge), the catalog ships to the browser, bloating Analyze and re-deriving findings that can drift from the HMAC-signed Run after a catalog sync.  
**Confirm:** open `lib/abap/catalog-service.ts` / `evidence-model.ts` / `cds-catalog.ts` and see a top-level import of generated JSON. If they already dynamic-import on the server only, drop this.

**8. Admin approve/reject is a GET with `token` in the query string**  
`admin/approve/page.tsx` and `admin/approve-tenant/page.tsx` — second `useEffect`  

`?action=reject` fires `handleReject()` immediately; `?auto=true` approves after 1s. `token` is read from the URL and posted to the API. Trigger: logged-in admin opens a mailed/crafted link (or any page that loads that URL as an image/referrer). Token lands in history, logs, and `Referer`. Even if the API checks the token, GET-triggered mutation + secret-in-query is the wrong shape.

**9. Dashboard “Developer Forum” is local React state**  
`dashboard/page.tsx` — `forumPosts` initial array + `handleCreateForumPost`  

Posts, likes, and comments never hit Firestore/API. Refresh restores the three hardcoded admin threads. Copy still says “posts publicly to the board” / “share insights with other SAP Clean-Core practitioners”. Trigger: any user opening the forum or posting. This is an unbackable product claim.

**10. MFA “QR code” is a decorative SVG**  
`settings/page.tsx` — `MockQrCode`  

Setup step 1 tells the user to scan it; the SVG ignores `qrCodeUrl` and draws a fixed pattern. Scanning it cannot enroll the `otpauth://` secret. Trigger: Enable 2FA → scan with Authenticator (typing the secret still works). Either render a real QR from `qrCodeUrl` or stop saying “scan”.

---

## Medium

**11. Uploaded ABAP examples are stored and then thrown away**  
`dashboard/page.tsx`  

`confirmUploadExample` writes `abap_examples`; a listener fills `abapExamples`; the grid uses `const allExamples = STATIC_EXAMPLES`. Trigger: upload a `.abap` file — it never appears; delete UI never shows because every visible card is `isStatic`. Dead path that users can reach.

**12. Marketing figures the code does not compute**  
- `abap-custom-code-analysis/page.tsx`: sidebar **“80% faster code assessment”** — no artifact behind it.  
- Same page + `how-it-works/page.tsx` Map step + landing comparison row: hardcoded **“23,000+”** while `getCatalogStats()` is already used on `/` and `/sap-cloudification`. Those will drift on the next sync.  
- `clean-core-score/page.tsx`: “four pillars” includes **“TCO Optimization Potential”** and “predict your TCO savings”. The score path (`routeExtensibility` / `liveCleanCoreScore`) does not calculate TCO; `/tco` is a separate slider.

**13. Settings “Test Connection” after reload sends an empty password**  
`settings/page.tsx` — `handleTestS4Connection`  

Password is write-only and cleared after save. Test always POSTs `password: s4Password`. Testing page uses `useStoredCredentials: true`. Trigger: save S/4 config, reload Settings, click Test — handshake uses `''` unless the API secretly ignores it (it does not in this client).

**14. BYOT request flag does not re-render**  
`testing/page.tsx` — `handleRequestAccess`  

`profile.s4TenantAccessRequested = true` mutates the hook object in place. Trigger: submit access request — UI stays on the form until a profile refetch.

**15. RAP projects still described as Node.js**  
- Delivery hero: “Your modernized **Node.js** application…” (`delivery/page.tsx`).  
- Transformation “Insights” are always Express / TypeORM / XSUAA (`transformation/page.tsx`).  
- `/how-to` HowTo JSON-LD and phases are BTP/Node only.  

Trigger: `extensibilityRoute` contains `ABAP Cloud`.

**16. Analyze “Switch Track” writes only the project doc**  
`analyze/page.tsx` — Extensibility Router card  

`updateDoc({ extensibilityRoute: nextRoute })` does not update the signed Run. Downstream Design/Transform follow the flipped route; the audit pack still has the original recommendation. Trigger: click Switch Track after analysis.

**17. Knowledge FAQ overclaims live BTP security**  
`knowledge/page.tsx` — “How does Clean-Core.io secure Side-by-Side BTP integration?”  

States the product “configures secure tunnels” and “implements JWT validated by SAP XSUAA”. What exists is generated snippet text (`@sap/xssec` / helmet) plus a destination JSON paste. That FAQ will be scraped as a capability you do not operate.

**18. `handleProceed` treats score > 90 as “skip to Delivery”**  
`dashboard/page.tsx`  

A just-analyzed project with a high (and possibly live-inflated) score never goes through design/transform/test. Combined with finding 1, Delivery then fabricates QA stats. Trigger: Continue on a project with `cleanCoreScore > 90` and `status === 'analyzed'`.

---

## Low / confirm

**BPMN export does not escape `id` / `name` / `role`** (`documentation/page.tsx` `generateBPMN`). Only documentation text uses `escapeXML`. A Gemini id/name with `"` or `&` yields broken Signavio XML. Confirm with a node name containing `&`.

**A–D on Analyze** (`AbcdClassificationPanel`): architecture requires every surface to say the grade is not in the signed pack. Confirm the panel copy; the page itself does not.

**`profile.mfaBackupCodes` rendered in Settings** vs Trust page “hashed (scrypt + pepper)”. If the user doc really stores plaintext codes, Trust is wrong; if it stores hashes, this UI cannot work. Confirm the users schema / MFA verify API.

**First-run** names `Z_MATERIAL_STOCK_CALC` at “99 lines”. Confirm against `STARTER_EXAMPLES`; I do not have that module.

---

**React-correctness / server-client / secrets otherwise:** no `useEffect` loops that clearly break production; marketing catalog pages correctly stay server-side; no Gemini key in these files (calls go through `callGemini` — assumed `/api/gemini`). Admin mutations go through `/api/admin/*` with a Bearer token, which is the right split aside from the GET auto-action.